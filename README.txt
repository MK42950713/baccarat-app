Baccarat Pattern Matcher V7.4.6 - DB Save Hard Fix

핵심 수정:
1. 실전입력 DB 저장 실패 원인 해결
   - 기존 내부 DB 함수가 p.length === 20만 허용해서 30개/40개 패턴이 저장 직후 삭제됐음
   - 이제 p.length >= 20이면 DB 패턴으로 허용

2. 실전입력 DB 저장
   - 20개, 30개, 40개 전체 길이 그대로 저장
   - 20개로 자르지 않음
   - 저장 후 DB 개수 즉시 반영
   - localStorage 저장 반영

3. 기존 유지
   - 초기화 버튼은 추천 성과 요약 삭제하지 않음
   - 추천 자동 적중집계 유지
   - 최근 6개 완전일치 유지
   - 실전 입력 40칸 유지
